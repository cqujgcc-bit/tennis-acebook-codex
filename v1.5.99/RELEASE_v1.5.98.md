# AceBook v1.5.98 结算到账修复

- `ACCEPTED` 转账复用原微信商户单号继续推进，不再永久停留在“结算处理中”。
- 只有原转账单明确进入 `FAIL` / `CANCELLED` 终态后才创建新单，消除重复打款窗口。
- 原单缺少 `package_info` 时先安全撤销，等待撤销完成后再继续收款。
- 钱包打开时主动查询微信转账终态；即使回调延迟或丢失，微信返回 `SUCCESS` 后也会自动同步为已结算。
- “重试打款”改为“继续收款”，明确先查原单、需要时由发起人在微信确认收款。
- 增加精确到单场球局的安全恢复脚本，用于处理线上历史卡单。
