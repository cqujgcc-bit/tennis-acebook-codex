# v1.5.51 — 修复管理员初始化(重新部署)

> 日期：2026-07-16
> 基于 v1.5.50

## 修复
- ✅ 确认 `claimAdmin` 正确位于 `admin: router({...})` 内部
- ✅ 重新创建独立版本部署目录 v1.5.51
- ✅ 触发全新 GitHub Actions 部署（后端 + 小程序）

## 背景
v1.5.50 已将 `claimAdmin` 从 `venue: router({...})` 移到 `admin: router({...})`，
使路径 `admin.claimAdmin` 正确可用。但由于部署可能未成功，此版本重新推送触发部署。

## 涉及文件
| 文件 | 改动说明 |
|------|---------|
| `v1.5.51/` | 新独立版本目录（基于 v1.5.50 复制）|
| `CODEX_CONTEXT.md` | 更新为 v1.5.51 |

## 使用方法
1. 进入"我的"页面 → 点击"激活管理员"
2. 输入授权码：`ACEBK-SETUP-0716`
3. 系统将当前用户设为管理员（一次性，已有管理员则失效）
